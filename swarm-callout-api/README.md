# Idea

- use a github gist as a public API to retrieve the current alpha-swarms in pokemmo from
- the source of the data is the official PokeMMO discord server, where users manually create alerts
- any discord server can follow this announcement channel to receive the same alerts -> a custom discord-bot on a private discord server could upload these alerts into a github gist where it could be automatically retrieved by any program with a simple github-raw API request

# API request using rust

// TODO
